import React from "react";

import AuthenticationAttributes from "./AuthComponents/AuthenticationAttributes";

const auth = () => (
  <div className="section section--compressed">
    <AuthenticationAttributes />
  </div>
);

export default auth;
