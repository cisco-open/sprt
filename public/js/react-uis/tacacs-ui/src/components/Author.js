import React from "react";

import AuthorizationAttributes from "./AuthComponents/AuthorizationAttributes";

const auth = () => (
  <div className="section section--compressed">
    <AuthorizationAttributes />
  </div>
);

export default auth;
